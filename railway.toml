[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd backend && npm install && node server-restored.js"

# Volumen persistente para base de datos SQLite
[[mounts]]
source = "/data"
destination = "/data"
mountType = "volume"